
Simply, sometimes there' might not be space in the allocated buffer for the shellcode. So we store our shellcode somewhere else in the memory of that program. 

For that we send our eggs : "w00tw00t" and our hunters : to hunt down the eggs and the shellcode.

